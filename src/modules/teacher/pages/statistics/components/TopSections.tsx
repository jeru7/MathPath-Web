import { type ReactElement } from "react";

export default function TopSections(): ReactElement {
  return (
    <article className="rounded-sm bg-white shadow-sm p-2 flex-1">
      <header className="font-semibold text-lg">Top Sections</header>
    </article>
  );
}
