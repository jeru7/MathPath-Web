import { type ReactElement } from "react";

export default function StagesStatistics(): ReactElement {
  return (
    <article className="bg-white shadow-sm p-2 rounded-md flex-1">
      <header className="font-semibold text-lg">Stages</header>
    </article>
  );
}
