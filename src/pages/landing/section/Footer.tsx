import { type ReactElement } from "react";

export default function Footer(): ReactElement {
  return (
    <section className="font-jersey flex h-fit w-screen items-center justify-center bg-[var(--primary-green)] text-[var(--primary-black)]">
      <h3>FOOTER</h3>
    </section>
  );
}
