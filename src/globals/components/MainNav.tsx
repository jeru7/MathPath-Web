import { type ReactElement } from "react"

export default function MainNav(): ReactElement {
    return <nav className="fixed bottom-0 left-0 z-10 h-screen w-16 rounded-r-lg bg-[var(--tertiary-green)] drop-shadow-lg"></nav>
}
