import { type ReactElement } from "react";

export default function About(): ReactElement {
  return (
    <section className="w-full h-screen p-24">
      <div className="bg-[var(--color-green-secondary)] w-full h-full border-2 rounded-4xl"></div>
    </section>
  );
}
